{
  "installCommand": "npm install",
  "buildCommand": "npm run build && ls -la",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}